#include <iostream>
#include <algorithm>
#include <stack>
using namespace std;
const int N = 1e5;
int n, m, t;
int main() {
    cin >> t;
    while (cin >> n >> m) {
        if (m == 0) {
        }
    }
}
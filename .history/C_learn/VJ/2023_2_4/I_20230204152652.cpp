#include <iostream>
#include <algorithm>
#include <stack>
#include<vector>
using namespace std;
const int N = 1e5;
int n, m, t, tot;

int main() {
    cin >> t;
    stack<int> s;
    while (cin >> n >> m) {
        if (m == 0) {
            s.push(++tot);
        }
    }
}
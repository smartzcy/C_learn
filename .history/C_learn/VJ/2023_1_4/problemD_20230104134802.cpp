#include<iostream>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int T;
    cin >> T;
    while(T--){
        int er[64];
        long long n , m , num;
        cin >> n >> m;
        for(int i = 0 ; i < m;i++){
            cin >> num;
            
        }
    }
}
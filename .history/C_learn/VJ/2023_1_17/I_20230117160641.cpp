#include <iostream>
#include <vector>
using namespace std;
const int N = 100;
int n, m;
int d[N], c[N];
int main() {
    cin >> n >> m;
    for (int i = 1; i <= n; i++)
        cin >> c[i];

    for (int i = 1; i <= n; i++)
        cin >> d[i];
}
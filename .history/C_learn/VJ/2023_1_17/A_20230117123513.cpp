#include <iostream>
#include <queue>
using namespace std;
int n, q;
int x1, y1, x2, y2;
string str[2];
void bfs(int u, int v) {
}
int main() {
    cin >> n >> q;
    cin >> str[0] >> str[1];
    while (q--) {
        int u, v;
        cin >> u >> v;
    }
}
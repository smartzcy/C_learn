#include <iostream>
#include <algorithm>
using namespace std;
const int N = 1e5;
int a[N];
int n, m, r;
int main() {
    cin >> n >> m >> r;
    for (int i = 1; i <= n; i++) cin >> a[i];
    for (int i = 1; i < n; i++) {
    }
}
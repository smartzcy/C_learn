#include <iostream>
#include <algorithm>
using namespace std;
const int N = 1e5;
int n, k;
int main() {
    // freopen("maxflow.in", "r", stdin);
    // freopen("maxflow.out", "w", stdout);
    cin >> n >> k;
    for (int i = 1, x, y; i < n; i++) {
        cin >> x >> y;
    }
}
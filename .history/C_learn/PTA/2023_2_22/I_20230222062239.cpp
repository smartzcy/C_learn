#include <iostream>
#include <algorithm>
#include <map>
using namespace std;
const int N = 1e5;
int n, m;
int main() {
    int T;
    cin >> T;
    while (T--) {
        string name;
        int k;
        cin >> name >> k;
        for (int i = 1, num; i <= k; i++) {
            cin >> num;
        }
    }
}
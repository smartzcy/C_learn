#include <iostream>
#include <cstdio>
#include <algorithm>
using namespace std;
const int N = 1e5;
int n, m;
int main() {
    string year, month, day;
    scanf("%s-%s-%s", day, month, year);
    printf("%s-%s-%s", year, month, day);
}